version: '3.8'

services:
  servergo:
    build: .
    container_name: servergo
    restart: always
    ports:
      - "50051:50051"
      - "8090:8090"
    environment:
      - SERVER_IP=209.126.81.68
      - GRPC_PORT=50051
      - WEB_PORT=8090
